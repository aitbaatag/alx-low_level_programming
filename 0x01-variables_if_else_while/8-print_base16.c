#include <stdio.h>

/**
 * main - Entry point for Hexadecimal numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int x;
for (x = 0; i <= 9; x++)
{
putchar(x + '0');
}
for (x = 'a'; i <= 'f'; x++)
{
putchar(x);
}
putchar('\n');
return (0);
}
